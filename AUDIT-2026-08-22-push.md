# تدقيق مسار الإشعارات (Push) — 2026-08-22

فحص كامل لمسار إشعارات الدفع في بوابة بلدية وادي الست: التسجيل على الجهاز،
التخزين، الاستهداف، الإرسال، التسليم، والتنظيف — مع اختبارات حيّة على المشروع
العامل (`onjbwhkmmtqnymhjnplw`).

لا يحتوي هذا الملف على أي بيانات شخصية: الأرقام أدناه إجماليات فقط.
(الملف منشور على نطاق البلدية مثل بقيّة ملفات المستودع.)

---

## الخلاصة بسطر واحد

**الإشعارات لا تصل.** البثّ إلى المقيمين مرفوض من الخادم منذ ترحيل الأمان `19`
لأن الشاشات ترسل مفتاح `anon` بدل جلسة المستخدم، وتنبيهات البلدية الداخلية
(«طلب جديد») لا تجد جهازاً واحداً مسجّلاً كموظّف. مسار التسليم نفسه سليم — أُثبت
بإرسال حقيقي وصل إلى جهاز خلال هذا التدقيق.

---

## ما تمّ فحصه

| الطبقة | الملف / المكوّن |
|---|---|
| التسجيل على الجهاز | `pwa.js` (`enablePush`, `linkToUser`, `autoLink`), `pwa-ios.js` |
| عامل الخدمة | `sw.js` — `push`, `notificationclick`, `pushsubscriptionchange`, `resolveNotificationUrl` |
| التخزين | `public.push_subscriptions`, `public.push_log`, `public.push_templates` |
| الصلاحية | `public.push_send_allowed` (ترحيل 19), سياسات RLS |
| الإرسال | دالة `send-push` (نسخة 15، `verify_jwt=false`) |
| الشاشات المُرسِلة | `admin-push.html`, `news-admin.html`, `bus-trip.html`, `citizen.html`, `dashboard.html` |

### الاختبارات الحيّة

| # | الاختبار | النتيجة |
|---|---|---|
| T1 | تنبيه داخلي `to_role:'staff'` (ما يُرسَل عند تقديم معاملة) | **200 — `recipients_count: 0`** «لا مشتركين مطابقين» |
| T2 | الاختبار نفسه بعد وسم جهاز فعّال واحد كـ `staff` | **200 — `recipients_count: 1, success: 1`** ووصل الإشعار فعلاً |
| T3 | بثّ خارجي بلا هويّة | **403** «إرسال إشعار للمقيمين يتطلّب صلاحية push_send» ✅ |
| T4 | محاكاة نداء `admin-push.html` بالحرف (مفتاح anon في الترويسة) | **403** — أي أن شاشة الإشعارات لا تستطيع الإرسال |

---

## النتائج

### 🔴 P1 — البثّ إلى المقيمين مُعطَّل منذ ترحيل الأمان

`admin-push.html:616` و`news-admin.html:669` تُرسلان:

```js
headers:{ 'Authorization':'Bearer '+SUPABASE_KEY, 'apikey':SUPABASE_KEY }
```

`SUPABASE_KEY` هو مفتاح **anon**، لا جلسة المُرسِل. دالة `send-push` تستخرج
هويّة المُنادي من هذا الـ JWT عبر `/auth/v1/user`؛ مفتاح anon لا يُعيد مستخدماً،
فيصير `callerId = null`، والنطاق `outward`، فترفض `push_send_allowed(null,'outward')`
والنتيجة **403** لكل بثّ.

- أُعيد إنتاجه حرفياً في T4.
- شاهد في البيانات: آخر بثّ وصل إلى أحد كان **2026-07-31** (٤ إرساليات، ٤٥ مستلماً
  إجمالاً). لا سطر بثّ واحد بعده — والرفض بـ 403 لا يُنشئ سطراً في `push_log` أصلاً،
  فالفشل صامت تماماً.
- `bus-trip.html:535` تفعلها **بشكل صحيح** (`session.access_token`) وهي المرجع.

**الإصلاح:** سطر واحد في كل شاشة — استعمال رمز الجلسة بدل مفتاح anon. وإن لم
يكن للمُرسِل صلاحية `push_send` فسيرى رفضاً مفهوماً بدل صمت.

### 🔴 P1 — تنبيهات البلدية الداخلية لا تصل إلى أحد

خلال ٦٠ يوماً: **٣٥ إرسالاً إلى `to_role:'staff'`** و**١٨ إلى `to_role:'admin'`**،
كلّها بـ `recipients_count = 0` — آخرها 2026-08-21 16:49. أي أن إشعار «طلب جديد
من مواطن» لم يصل إلى هاتف أي موظّف منذ شهور.

السبب: لا اشتراك **فعّال** واحد يحمل `user_role = 'staff'` أو `'admin'`. ثلاثة
عوامل متضافرة:

1. **لا شاشة موظّفين تطلب الإشعارات أصلاً.** `enablePush` تُنادى من صفحات
   المواطنين فقط (`index`, `news`, `phonebook`, `citizen`, `bus-trip`).
   `dashboard.html` تكتفي بإعادة وسم اشتراك موجود (`if status === 'subscribed'`)،
   فالموظّف الذي لم يفعّل الإشعارات من صفحة عامة لا يملك اشتراكاً إطلاقاً.
2. **الوسم لا يثبت.** `autoLink()` تعمل عند كل تحميل صفحة وتنادي
   `linkToUser(u.phone, u.name, u.role || 'citizen')` — فتُعيد وسم جهاز الموظّف
   **مواطناً** عند أوّل زيارة لصفحة عامة.
3. **بصمة منع التكرار تتجاهل الدور**: `wadi_push_linked = phone|name`، فتغيّر
   الدور وحده لا يُطلق إعادة ربط.

**الإصلاح:** طلب الإذن من لوحة البلدية نفسها؛ عدم تمرير `'citizen'` كقيمة
افتراضية فوق دور قائم؛ وإدخال الدور في بصمة منع التكرار.

### 🟠 P2 — أي شخص يستطيع محو اشتراكات القرية كلّها أو العبث بها

سياسات `push_subscriptions`:

| السياسة | العملية | الأدوار | الشرط |
|---|---|---|---|
| `ps_device_insert` | INSERT | anon, authenticated | `true` |
| `ps_device_update` | UPDATE | anon, authenticated | `USING true` / `CHECK true` |
| `ps_device_delete` | DELETE | anon, authenticated | `USING true` |

مفتاح anon منشور في كل صفحة. بهذه السياسات يستطيع أي شخص:

- `delete from push_subscriptions` — محو الـ ٢٣ اشتراكاً دفعة واحدة،
- أو `update … set is_active = false` — إسكات قناة البلدية كلّها بلا أثر ظاهر،
- أو تغيير `topics` / `user_phone` / `user_role` لأي جهاز.

القراءة **ليست** مكشوفة (لا سياسة SELECT لـ anon)، فالخطر تخريبي لا استخباري.

**الإصلاح المقترح:** إبقاء INSERT مفتوحاً (الجهاز يسجّل نفسه)، ونقل التحديث
والحذف إلى دالتين `security definer` تأخذان `endpoint` الجهاز — فلا يلمس أحد
سطراً لا يملك عنوانه.

### 🟠 P2 — سجلّ الإشعارات مقروء لكل حساب مُسجَّل دخوله

`pl_select_authenticated` شرطها `auth.uid() is not null` — أي أن أي حساب مواطن
يقرأ الـ ١٦٠ سطراً في `push_log` بكامل نصوصها وعمود `to_user_phone`.

**الإصلاح:** قصر القراءة على `has_perm('push_send')` أو أدوار البلدية.

### 🟡 P3 — ثلاث صيغ للهاتف في عمود واحد، والمطابقة حرفية

`user_phone` يحوي اليوم صيغاً مختلفة (وطنية بصفر، بلا صفر، دولية `+961…`،
وسلسلة فارغة). و`send-push` تطابق بالتساوي التام
(`user_phone.eq.X` أو `+X`) بعد تجريد غير الأرقام فقط. فإن كان رقم المعاملة
مخزَّناً `+9613…` والاشتراك `03…` **لا يتطابقان**، ويذهب الإشعار إلى لا أحد
بصمت — وهذا ما حصل في إرسالية 2026-08-20 (`recipients_count = 0`).

**الإصلاح:** التطبيع عند الكتابة (`phoneE164` الموجودة الآن في `number-format.js`)
والمطابقة على «الأرقام بعد إسقاط 961 والصفر» تماماً كما تفعل دوال التعاونية.

### 🟡 P3 — `enablePush` تكتب في عمود لا يقرأه أحد

الجدول يحمل عمودَي `role` و`user_role`. الاستهداف يقرأ `user_role`، بينما
`enablePush` تكتب `role`. العمود `role` فارغ في الأسطر الـ ٢٣ كلّها ولا يقرأه شيء.

**الإصلاح:** الكتابة في `user_role`، وحذف العمود الميت.

### 🟡 P3 — بريد إلكتروني يُمرَّر مكان رقم هاتف

`dashboard.html:1226` → `PWA.linkToUser(user.email, userName, 'staff')`.
داخل `linkToUser` يُجرَّد كل ما ليس رقماً، فيصبح البريد سلسلة فارغة وتُكتب في
`user_phone`. (هذا مصدر الاشتراك الوحيد ذي الهاتف الفارغ.)

### ⚪ P4 — لا اشتراك جديد منذ ٢٠٢٦-٠٥-٢٣

٢٣ اشتراكاً إجمالاً، **١٠ فعّالة**، وآخر تسجيل جهاز جديد قبل ثلاثة أشهر، رغم أن
البوابة تُستعمل يومياً. لا تليمتري في الواجهة يفسّر السبب (رفض الإذن؟ عدم ظهور
الدعوة؟ iOS بلا تثبيت؟)، والتوصية هي تسجيل نتيجة `enablePush` — نجاحاً وفشلاً —
كي يصبح السؤال قابلاً للقياس.

---

## ما هو سليم فعلاً

- **التسليم يعمل**: T2 أرسل إشعاراً حقيقياً وصل إلى جهاز، و`last_used_at` خُتم،
  و`failed_count = 0`. مفاتيح VAPID مضبوطة و`web-push` يوقّع سليماً.
- **بوّابة الصلاحية تعمل كما صُمِّمت**: `inward` مفتوح، `outward` يتطلّب
  `push_send` — رُفض الاختباران T3 وT4 بـ 403 وبرسالة عربية مفهومة.
- **تنظيف العناوين الميتة يعمل**: 404/410 يُعطّل الاشتراك (١٣ اشتراكاً مُعطَّلاً،
  و`failed_count = 0` عليها جميعاً)، وخمس محاولات فاشلة تُعطّل تدريجياً.
- **`push_log` يسجّل كل إرسال** بعدد المستلمين والنجاح والفشل والمُرسِل.
- **عامل الخدمة سليم**: `resolveNotificationUrl` يحلّ الروابط على نطاق التطبيق لا
  على الأصل، ويسقط إلى `news.html` عند الشكّ، ويرفض النطاقات الغريبة؛ والإشعار
  يُبنى بـ `dir: rtl` و`lang: ar`.

---

## الترتيب المقترح للإصلاح

| # | العمل | الأثر |
|---|---|---|
| 1 | تمرير رمز الجلسة في `admin-push.html` و`news-admin.html` | يُعيد البثّ إلى الحياة |
| 2 | تفعيل الإشعارات من لوحة البلدية + منع `autoLink` من دهس دور الموظّف | تصل تنبيهات «طلب جديد» |
| 3 | إغلاق UPDATE/DELETE على `push_subscriptions` | يمنع محو القناة |
| 4 | قصر قراءة `push_log` على البلدية | يمنع تسرّب الأرقام والنصوص |
| 5 | تطبيع الهاتف عند الكتابة والمطابقة | يوقف الإخفاقات الصامتة |
| 6 | الكتابة في `user_role` وحذف `role` | يزيل فخّاً |

---

## أثر جانبي لهذا التدقيق

لإثبات التسليم (T2) وُسم **جهاز واحد فعّال** بـ `user_role = 'staff'` بدل
`'citizen'`. هذا هو الوضع الصحيح لصاحبه (وهو من البلدية)، ولذلك تُرك كما هو —
لكن `autoLink()` ستُعيده إلى `'citizen'` عند أوّل زيارة لصفحة عامة إلى أن يُنفَّذ
الإصلاح رقم ٢. لم يُغيَّر أي اشتراك آخر، ولم تُحذف أي بيانات.
